
/*

var user;



function getLocation() {


            if (navigator.geolocation) {
                navigator.geolocation.getCurrentPosition(sendCoord);
            }
                else {
                    user.innerHTML = "Location services currently not available."
                };
}

            function sendCoord

*/